<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Your_brain - Sign Up</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-zinc-900 flex items-center justify-center min-h-screen p-4">
    <div class="bg-zinc-800 w-full max-w-md sm:max-w-lg md:max-w-xl lg:max-w-2xl xl:max-w-3xl p-6 rounded-xl shadow-lg">
      <form method="post" action="/create" enctype="multipart/form-data" class="flex flex-col space-y-4">
        <h2 class="text-white text-2xl font-semibold text-center mb-4">Create an Account</h2>

        <input
          type="text"
          name="name"
          placeholder="Name"
          class="w-full p-3 bg-zinc-700 rounded-md text-white focus:outline-none focus:ring-2 focus:ring-blue-500"
          required
        />

        <input
          type="email"
          name="email"
          placeholder="Email"
          class="w-full p-3 bg-zinc-700 rounded-md text-white focus:outline-none focus:ring-2 focus:ring-blue-500"
          required
        />

        <input
          type="number"
          name="age"
          placeholder="Age"
          class="w-full p-3 bg-zinc-700 rounded-md text-white focus:outline-none focus:ring-2 focus:ring-blue-500"
          required
        />

        <input
          type="password"
          name="password"
          placeholder="Password"
          class="w-full p-3 bg-zinc-700 rounded-md text-white focus:outline-none focus:ring-2 focus:ring-blue-500"
          required
        />

        <input
          type="file"
          name="image"
          accept="image/*"
          class="w-full p-3 bg-zinc-700 rounded-md text-white focus:outline-none focus:ring-2 focus:ring-blue-500"
          required
        />

        <input
          type="submit"
          value="Create Account"
          class="w-full bg-blue-600 hover:bg-blue-500 text-white font-semibold py-3 rounded-md cursor-pointer transition duration-200"
        />
      </form>

      <!-- Login Option -->
      <p class="text-center text-white mt-4">
        Already have an account?
        <a href="/login" class="text-blue-400 hover:underline">Log in</a>
      </p>
    </div>
  </body>
</html>
